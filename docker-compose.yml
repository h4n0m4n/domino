services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LLM_PROVIDER=ollama
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    volumes:
      - ./scenarios:/app/scenarios

  web:
    build: ./web
    ports:
      - "3000:3000"
    depends_on:
      - api
